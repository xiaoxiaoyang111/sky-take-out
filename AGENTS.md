# 项目协作指南

本文件是本仓库中 AI 辅助开发的长期约定。每次开始任务时先阅读本文件，再结合用户当次要求执行；若两者冲突，以用户当次要求为准。

## 项目概况

- 项目：苍穹外卖后端，Spring Boot 2.7.3、Maven 多模块、MyBatis、MySQL、Redis。
- `sky-common`：通用常量、异常、工具、配置属性和统一返回对象。
- `sky-pojo`：entity、DTO、VO。
- `sky-server`：Controller、Service、Mapper、配置及应用入口。
- Java 根包为 `com.sky`；MyBatis XML 位于 `sky-server/src/main/resources/mapper`。
- 当前项目处于早期开发阶段，测试覆盖可能不完整，不能把“能编译”等同于“功能正确”。

## 本地联调环境

- 管理端前端入口：`http://localhost/#/login`。
- 前端由本机 Nginx 提供，目录为 `C:\AAAAA\One_Study\frontend\sky-takeout\nginx-1.20.2`，静态页面位于其 `html\sky` 目录。
- 启动前先检查 Nginx 是否已运行及 80 端口是否已监听；未运行时可在上述目录双击 `nginx.exe`，启动后确认首页返回 HTTP 200。不要因看到两个 `nginx.exe` 进程就重复启动，它们通常是 master/worker 进程。
- 后端默认监听 `8080`。Nginx 将管理端 `/api/` 转发到 `http://localhost:8080/admin/`，将 `/user/` 转发到后端用户端接口，并将 `/ws/` 转发到后端 WebSocket。
- 进行完整前后端联调时，应同时启动后端和 Nginx，再从上述登录页验证；只运行单元测试或直接请求 8080 后端接口时不要求启动 Nginx。
- 联调前分别检查 80、8080 端口和 Nginx 配置，避免把“后端未启动”“代理配置问题”误判为业务代码错误。

## 学习笔记关联

- Obsidian 仓库：`C:\AAAAA\One_Study\黑曜石笔记\苍穹外卖和黑马点评有感`。
- 本项目索引：`10-项目\苍穹外卖\00-项目索引.md`；复盘问题库：`10-项目\苍穹外卖\复盘问题库.md`。
- 涉及知识讲解、阶段复盘或学习记录时，先查看项目索引和最近的 `00-每日记录`；涉及 AOP、JWT、MyBatis、Redis 或事务时，同时查看 `20-公共知识`。
- 用户要求“同步笔记”时，记录调用链、设计原因、排错过程与验证结果；不要只复制代码。

## 每次任务先做什么

1. 阅读本文件、相关 `pom.xml`、配置和目标代码，确认模块边界及现有写法。
2. 查看 Git 状态和相关 diff；默认所有未提交修改都属于用户，不覆盖、不回退、不顺手整理。
3. 用一句话复述目标、验收条件和准备修改的范围；信息足够时直接工作，不反复追问。
4. 先复现问题或建立当前基线，再修改；无法运行时明确缺少的依赖、数据或环境。
5. 只做达成目标所需的最小改动，避免无关重构、批量格式化或随意升级依赖。

## 开发约定

- 保持 Controller → Service → Mapper 的职责边界；DTO 用于入参，VO 用于出参，Entity 对应持久化数据。
- 沿用统一的 `Result`、`PageResult`、业务异常和全局异常处理，不在接口中临时拼装另一套响应格式。
- SQL 简单且静态时可使用注解；动态 SQL 或字段较多时优先使用对应 Mapper XML，并确保方法名、参数和映射一致。
- 新增或更新带审计字段的数据时，优先复用 `@AutoFill`、`BaseContext` 和既有常量。
- 遵循现有包名和命名风格；新增公共能力前先搜索是否已有实现。
- 不记录密码、令牌、身份证号等敏感信息；不把真实密钥或生产配置写入仓库。发现现有明文配置时先提示风险，除非任务要求，否则不要擅自改动运行环境。
- 修复缺陷时优先补回归测试；新增功能至少覆盖正常路径、关键边界和主要失败路径。
- 若新增测试文件，注意当前 `.gitignore` 会忽略 `*Test.java` 和 `**/test/`，应说明影响并在用户允许的范围内调整。

## 验证顺序

按改动风险选择足够的验证，不跳过可执行的步骤：

1. 运行与改动最相关的单元或集成测试。
2. 运行受影响模块测试，例如 `mvn -pl sky-server -am test`。
3. 跨模块或公共代码变更时运行全项目 `mvn test`；需要时再运行 `mvn package`。
4. 涉及接口、数据库、Redis、JWT 或外部服务时，补充启动/接口验证；外部依赖不可用时使用可说明的替代验证，不伪造成功。
5. 涉及管理端页面流程时，通过 `http://localhost/#/login` 做真实联调，检查页面操作、浏览器请求、接口响应和后端日志是否一致。
6. 检查最终 diff，确认没有调试代码、意外文件、敏感信息或无关改动。

## 完成标准

只有同时满足以下条件才称为“完成”：

- 需求和验收条件已实现，原有兼容行为未被无意破坏。
- 相关测试已新增或更新，并且可运行的验证均通过。
- 失败分支、空值、权限、事务和数据一致性已按任务风险检查。
- 最终回复说明：改了什么、为何这样改、验证结果、仍存在的限制或需要用户执行的步骤。
- 不隐瞒失败，不把未执行的测试写成已通过。

## 与用户协作

- 默认使用中文，先给结论，再给必要细节。
- 需求有小歧义时采用最保守、可逆的合理假设并明确说明；会明显改变业务行为、数据结构或外部系统时再询问用户。
- “分析/审查/诊断”只读取并报告，不自动改代码；“实现/修复/修改”才执行代码变更和验证。
- 遇到阻塞先完成不依赖该阻塞的检查，然后给出具体证据和最少必要问题。

## 推荐提问模板

复制下面模板并按需删减：

```text
任务类型：实现功能 / 修复缺陷 / 补测试 / 重构 / 代码审查 / 讲解
目标：我希望最终实现什么。
现状：目前做到哪里，出现了什么现象或报错。
范围：允许修改哪些模块或文件；哪些内容不能动。
业务规则：输入、输出、权限、状态变化、异常处理等要求。
验收标准：满足哪些可观察结果才算完成。
验证方式：希望运行哪些测试、接口或启动检查。
交付要求：是否需要直接修改代码、只给方案、生成文档或提交说明。
补充材料：报错日志、接口文档、数据库结构、示例数据或截图。

请先检查现有实现和 Git 修改，保护我未提交的代码；然后完成修改、测试、复查，并在最后汇总改动、验证结果和遗留问题。
```

### 简短提问示例

```text
请实现员工手机号唯一性校验。允许修改 sky-server 和相关测试，不改变现有响应结构。新增与编辑都要校验，编辑时排除本人；重复时返回现有业务异常。请先检查当前实现，再直接修改并运行相关测试，最后告诉我改动和验证结果。
```
